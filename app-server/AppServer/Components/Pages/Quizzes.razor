@page "/quizzes"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Redirect to /myquizzes (without forceLoad to preserve Circuit)
        Nav.NavigateTo("/myquizzes");
    }
}
