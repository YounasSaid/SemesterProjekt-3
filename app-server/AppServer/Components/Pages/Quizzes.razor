@page "/quizzes"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Redirect to /myquizzes
        Nav.NavigateTo("/myquizzes", forceLoad: true);
    }
}
