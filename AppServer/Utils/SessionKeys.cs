namespace AppServer.Utils;

/// <summary>
/// Konstanter for session nøgler.
/// Bruges til at gemme bruger information i HTTP session storage.
/// </summary>
public static class SessionKeys
{
    public const string UserId = "session.userId";
}
